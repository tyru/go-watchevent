on_create: post-libresonic-rescan
on_remove: post-libresonic-rescan

action:
  - name: post-libresonic-rescan
    interval: 5s
    interval_action:
      - on: ['self']
        do: cancel
      - on: ['!self']
        do: retry
    run: echo $(date) $WEV_EVENT $WEV_PATH >>date.txt
